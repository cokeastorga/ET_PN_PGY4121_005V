import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.constanza.objetosperdidos',
  appName: 'ObjetosPerdidos',
  webDir: 'www'
};

export default config;
